import React from 'react';

interface KeyDataProps {}

/**
 * @param {React.Props<KeyDataProps>}
 * @returns {React.ReactElement}
 */
const KeyData: React.FC<KeyDataProps> = (props): React.ReactElement => {
  return <div></div>;
};

export default KeyData;
